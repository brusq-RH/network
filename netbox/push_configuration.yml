---

- name: Push data on the device
  hosts: localhost
  connection: local

  gather_facts: false
  tasks:

    - name: Device
      debug:
        message: "device {{device_id}}"

    - name: interface
      debug:
        message: "device {{interface_id}}"
